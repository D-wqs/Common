<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.stamper.yx.common.mapper.sqlite.AddrMapper">
    <resultMap id="BaseResultMap" type="com.stamper.yx.common.entity.AddrInfo">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="create_date" property="createDate" jdbcType="VARCHAR"/>
        <result column="update_date" property="updateDate" jdbcType="VARCHAR"/>
        <result column="delete_date" property="deleteDate" jdbcType="VARCHAR"/>
        <result column="device_id" property="deviceId" jdbcType="INTEGER"/>
        <result column="longitude" property="longitude" jdbcType="VARCHAR"/>
        <result column="latitude" property="latitude" jdbcType="VARCHAR"/>
        <result column="location" property="location" jdbcType="VARCHAR"/>
    </resultMap>
    <sql id="Base_Column_List">
        create_date,update_date,device_id,longitude,latitude,location
    </sql>
    <insert id="insert" parameterType="com.stamper.yx.common.entity.AddrInfo" useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO addr (
        <include refid="Base_Column_List"/>
        )
         VALUES(#{createDate},#{updateDate},#{deviceId},#{longitude},#{latitude},#{location})
    </insert>
    <update id="update">
        update addr
        <set>
            <if test="createDate != null">
                create_date = #{createDate,jdbcType=TIMESTAMP},
            </if>
            <if test="updateDate != null">
                update_date = #{updateDate,jdbcType=TIMESTAMP},
            </if>
            <if test="deleteDate != null">
                delete_date = #{deleteDate,jdbcType=TIMESTAMP},
            </if>
            <if test="deviceId != null">
                device_id = #{deviceId,jdbcType=INTEGER},
            </if>
            <if test="longitude != null">
                longitude = #{longitude,jdbcType=VARCHAR},
            </if>
            <if test="latitude != null">
                latitude = #{latitude,jdbcType=VARCHAR},
            </if>
            <if test="location != null">
                location = #{location,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <select id="getAllBySignet" parameterType="int" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
            addr
        WHERE
            delete_date IS NULL
        AND device_id=#{deviceId,jdbcType=INTEGER}
        ORDER BY update_date
    </select>
    <select id="getById" parameterType="int" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List"/>
        FROM
            addr
        WHERE
            delete_date IS NULL
            AND id =#{id,jdbcType=INTEGER}
            LIMIT 1
    </select>

</mapper>